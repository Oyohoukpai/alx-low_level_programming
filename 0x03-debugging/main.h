#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
void print_remaining_days(int month, int day, int year);
int largest_number(int a, int b, int c);
void positive_or_negative(int i);
int largest_number(int a, int b, int c);
int convert_day(int month, int day);
void print_remaining_day(int month, int day, int year);
void print_remaining_days(int month, int day, int year);
int convert_days(int month, int day);
int largest_numbers(int a, int b, int c);
int print_largest_number(int a, int b, int c);

#endif /* MAIN_H */
#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

void positive_or_negative(int i);
int largest_number(int a, int b, int c);
void print_remaining_days(int month, int day, int year);
int convert_day(int month, int day);

#endif /* MAIN_H */
