#include <unist.h>
#include <stdio.h>
/**
 * main - print the alphabet at reverse
 * Return: Always (Success)
 */
  int main(void)
  {
 int a;

  for (a  = 'z'; a >= 'a'; a--)
  {
  putchar(a);
  }
  putchar('\n');
	  return (0);
  }
